# Booker

## Como rodar o projeto

### Requisitos
- Java 21
- Docker
- Arquivo .env

Rode o comando:

``` docker-compose up --build ```

Uma vez que o container for construído, basta usar apenas:

``` docker-compose up ```